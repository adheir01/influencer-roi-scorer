name: 'influencer_roi'
version: '1.0.0'
config-version: 2

profile: 'influencer_roi'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  influencer_roi:
    staging:
      +materialized: view
    marts:
      +materialized: table
